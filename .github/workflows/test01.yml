name: Mirror Upstream (Force Except Workflows index & Robots)

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  mirror:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout fork
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: master

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Save local workflows, robots & index
        run: |
          mkdir -p /tmp/backup
          
          if [ -d ".github/workflows" ]; then
            cp -r .github/workflows /tmp/backup/
          fi
          
          if [ -f "functions/robots.txt.js" ]; then
            mkdir -p /tmp/backup/functions
            cp functions/robots.txt.js /tmp/backup/functions/
          fi

          if [ -f "index.html" ]; then
            cp index.html /tmp/backup/
          fi

      - name: Add upstream
        run: |
          git remote add upstream https://github.com/nengmaya538/nengmaya538.github.io.git || true
          git fetch upstream

      - name: Hard reset to upstream
        run: |
          git reset --hard upstream/master

      - name: Restore workflows, robots & index
        run: |
          rm -rf .github/workflows
          if [ -d "/tmp/backup/workflows" ]; then
            mkdir -p .github
            cp -r /tmp/backup/workflows .github/
          fi

          if [ -f "/tmp/backup/functions/robots.txt.js" ]; then
            mkdir -p functions
            cp /tmp/backup/functions/robots.txt.js functions/
          fi

          if [ -f "/tmp/backup/index.html" ]; then
            cp /tmp/backup/index.html index.html
          fi

      - name: Force push to fork
        run: |
          git add .
          git commit -m "Force sync except workflows, robots & index.html" || echo "No changes"
          git push origin master --force
